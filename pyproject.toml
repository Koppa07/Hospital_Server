[project]
name = "pis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Koppa07", email = "ksesyolkina@mail.ru" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
    "django-filter>=26.1",
    "djangorestframework>=3.18.1",
    "psycopg2>=2.9.12",
    "psycopg2-binary>=2.9.12",
]

[project.scripts]
pis = "pis:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
